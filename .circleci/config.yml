version: 2
jobs:
  test:
    working_directory: ~/repo
    docker:
      - image: circleci/node:10.14
        environment:
          PIPENV_VENV_IN_PROJECT: true
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-node-dependencies-{{ checksum "package.json" }}
            - v1-node-dependencies-
      - run: yarn install
      - save_cache:
          paths:
            - node_modules
          key: v1-node-dependencies-{{ checksum "package.json" }}
      - run: yarn test
  build_and_deploy:
    docker:
      - image: circleci/python:3.7-node
        environment:
          PIPENV_VENV_IN_PROJECT: true
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-node-dependencies-{{ checksum "package.json" }}
      - run: yarn install
      - restore_cache:
          keys:
            - v1-python-dependencies-{{ checksum "Pipfile" }}
            - v1-python-dependencies-
      - run:
          name: install dependencies
          command: |
            sudo pip install pipenv
            pipenv install
      - save_cache:
          paths:
            - .venv
          key: v1-python-dependencies-{{ checksum "Pipfile" }}
      - run: pipenv run yarn build
      - run: bash .circleci/deploy.sh
workflows:
  version: 2
  test_build_and_deploy:
    jobs:
      - test:
          filters:
            branches:
              ignore: master
      - build_and_deploy:
          requires:
            - test
          filters:
            branches:
              only: workspace
