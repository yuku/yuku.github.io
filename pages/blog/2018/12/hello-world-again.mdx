import BlogPage from "../../../../src/components/BlogPage/BlogPage"
import { INFO_COLOR } from "../../../../src/constants"

export const meta = {
  title: "Hello World Again",
  publishedAt: "2018-12-29T23:59:00+09:00",
  backgroundColor: INFO_COLOR,
}

export default ({ children }) => <BlogPage {...meta}>{children}</BlogPage>

Hello world

```js
function hello() {
  return "World"
}
```

Lift($L$) can be determined by Lift Coeeficient ($C_L$) like the following equation.

$$
H ( Y | X ) = \sum _ { x \in \mathcal { X } , y \in \mathcal { Y } } p ( x , y ) \log \left( \frac { p ( x ) } { p ( x , y ) } \right)
$$
