[RSS フィード](/static/rss-feed.xml) を実装しました。このご時世にどれほど使われるか不明ですが。

このブログではエントリのメタデータ一覧を entries.ts というファイルで管理しているので、そのファイルのデータから [rss](https://www.npmjs.com/package/rss) という npm パッケージを使って XML を作っています。

## おわりに

ブログ開発しても肝心の記事を書かないのでは無意味なので、もっと書いていきたいですね。

## 参考

- yuku/yuku.github.io#4: Add RSS feed
